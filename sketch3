size(500,500);
background(255);
colorMode(HSB,100);

int a = 35;
while (a < 480) {
  noStroke();   
  fill(random(100),100,100);
  ellipse(a,33,18,18);
    int b = 33;
    while (b < 480) {
      noStroke();
      fill(random(100),100,100);
      ellipse(a,b,18,18);
      b = b + 23;
    }
  a = a + 23;
 }
